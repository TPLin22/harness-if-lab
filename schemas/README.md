# Schemas

This directory is reserved for future machine-readable contracts. No formal
schema is introduced in the scaffold.

The eventual schemas should cover the conceptual boundaries described in
`docs/design.md` without making the semantic benchmark depend on Harbor or one
specific model provider.
