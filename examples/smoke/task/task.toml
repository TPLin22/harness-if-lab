schema_version = "1.0"

[task]
name = "harness-if-lab/smoke-user-project"
description = "Tiny task used to validate HIF surface delivery."
authors = [{ name = "Harness-IF Lab" }]
keywords = ["harness-if", "smoke"]

[verifier]
timeout_sec = 60
environment_mode = "shared"

[agent]
timeout_sec = 120

[environment]
build_timeout_sec = 300
cpus = 1
memory_mb = 1024
